<!DOCTYPE html>
<html>
  <head>
    <title>Frugal Fashion Tracker</title>
    <%= csrf_meta_tags %>

    <link rel="stylesheet"
    href="https://cdnjs.cloudflare.com/ajax/libs/foundation/6.4.1/css/foundation.min.css">
    <link href="https://fonts.googleapis.com/css?family=Kite+One" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css?family=Alegreya+Sans:400,700" rel="stylesheet">
    <%= javascript_include_tag 'application' %>
    <%= stylesheet_link_tag    'application', media: 'all' %>
  </head>

  <body>
    <div class="top-bar">
      <div class="top-bar-left">
        <ul class="dropdown menu" data-dropdown-menu>
          <li><%= link_to "Frugal Fashion Tracker", deals_path, method: :get %></li>
          <li><%= link_to "All Posts", all_posts_path, method: :get %>
            <ul class="menu vertical">
              <li><%= link_to "All Deals", deals_path, method: :get %></li>
              <li><%= link_to "All Reviews", reviews_path, method: :get %></li>
              <li><%= link_to "All Discussions", discussions_path, method: :get %></li>
            </ul>
          </li>
          <li><%= link_to "Custom Posts", custom_posts_path, method: :get %>
            <ul class="menu vertical">
              <li><%= link_to "Custom Deals", custom_deals_path, method: :get %></li>
              <li><%= link_to "Custom Reviews", custom_reviews_path, method: :get %></li>
              <li><%= link_to "Custom Discussions", custom_discussions_path, method: :get %></li>
            </ul>
          </li>
        </ul>
      </div>

      <div class="top-bar-right">
        <ul class="menu">
          <% if current_user %>
          <li><%= link_to "Sign Out", destroy_user_session_path, method: :delete %></li>
          <li><%= link_to "Edit Profile", edit_user_registration_path %></li>
          <li class="menu-text"><%= current_user.username %></li>
          <% if current_user.profile_photo == nil %>
          <% current_user.profile_photo = "/uploads/user/profile_photo/75/T-Shirt-icon.png" %>
          <% else %>
          <%= image_tag current_user.profile_photo, :class => "profile-photo" %>
          <% end %>
          <% else %>
          <li><%= link_to "Log In", new_user_session_path, method: :get %></li>
          <li><%= link_to "Sign Up", new_user_registration_path, method: :get %></li>
          <% end %>
        </ul>
      </div>
    </div>

    <div class="header">
      <% flash.each do |key, value| %>
      <div class=“alert alert-<%= key %>“><%= value %></div>
      <% end %>
    </div>

    <div class='row'>
      <%= yield %>
    </div>
  </body>
</html>






<!-- <nav class="top-bar" data-topbar role="navigation">
  <ul class="title-area">
    <li class="name">
      <h1><a href="#">Openbay</a></h1>
    </li>
  </ul>
  <section class="top-bar-section">
    <!Right Nav Section -->
    <!-- <ul class="right">
      <li class="has-dropdown">
        <a href="#">Michael</a>
        <ul class="dropdown">
          <li><a href="#">Profile</a></li>
          <li class="active"><a href="#">Sign Out</a></li>
        </ul>
      </li>
    </ul>
  </section> -->
<!-- </nav> -->
