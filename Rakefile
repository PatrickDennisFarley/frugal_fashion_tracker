# Add your own tasks in files placed in lib/tasks ending in .rake,
# for example lib/tasks/capistrano.rake, and they will automatically be available to Rake.

require_relative 'config/application'

Rails.application.load_tasks


# This is where to program the send-test logic
task :send_notifications do
  #all logic - conditional/send text logic etc
  #first thing to do is find a way to store the time the rake task took place so it can be referenced the next time the task is run. 
end
# run rake send-Notications in heroku every day at 12 oclock - Heroku Scheduler
